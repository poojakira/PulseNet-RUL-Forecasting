name: PulseNet CI

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint:
    name: Lint and Format
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
      - name: Set up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
      - name: Install Ruff
        run: pip install ruff==0.11.8
      - name: Run Ruff Linter
        run: ruff check src tests scripts verify.py
      - name: Run Ruff Formatter
        run: ruff format --check src tests scripts verify.py

  test:
    name: Tests and Coverage
    runs-on: ubuntu-latest
    needs: lint
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
      - name: Set up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pytest pytest-asyncio pytest-cov httpx
      - name: Download Official NASA C-MAPSS Dataset
        # The C-MAPSS archive is not committed (large binary, gitignored). Fetch
        # the public NASA dataset and verify its SHA-256 before tests/verify run.
        run: python scripts/download_data.py
      - name: Run Tests with Coverage Gate
        run: |
          PYTHONPATH=src pytest tests -q \
            --cov=src/pulsenet \
            --cov-report=term-missing \
            --cov-report=xml:coverage.xml \
            --cov-fail-under=80
      - name: Official NASA Data Verification
        run: PYTHONPATH=src python verify.py
      - name: Upload Coverage Report
        if: always()
        uses: actions/upload-artifact@65462800fd760344b1a7b4382951275a0abb4808
        with:
          name: coverage-report
          path: coverage.xml

  security:
    name: Security Audit
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
      - name: Set up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
      - name: Install dependencies
        run: |
          pip install -r requirements.txt
          pip install bandit==1.8.3 cyclonedx-bom==5.1.1 pip-audit==2.10.1
      - name: Bandit SAST
        run: bandit -r src/ -ll
      - name: pip-audit
        # NOTE: The vulnerable dependencies flagged by an earlier audit were
        # genuinely remediated by upgrading (python-multipart, requests,
        # python-jose, cryptography, mlflow, pyarrow, pytest, starlette 0.41->0.49).
        # The residual advisories below have NO fix compatible with this project
        # and are therefore explicitly accepted and documented in SECURITY.md:
        #   CVE-2025-3000      torch 2.12  - no patched release exists
        #   PYSEC-2026-1325    ecdsa       - no patched release (transitive via python-jose)
        #   PYSEC-2026-161/248/249/2280/2281  starlette - fixed only in starlette 1.x,
        #     which no released FastAPI supports (FastAPI pins starlette <0.50).
        run: >
          pip-audit -r requirements.txt --skip-editable
          --ignore-vuln CVE-2025-3000
          --ignore-vuln PYSEC-2026-1325
          --ignore-vuln PYSEC-2026-161
          --ignore-vuln PYSEC-2026-248
          --ignore-vuln PYSEC-2026-249
          --ignore-vuln PYSEC-2026-2280
          --ignore-vuln PYSEC-2026-2281
      - name: Generate CycloneDX SBOM
        run: cyclonedx-py environment --output-format JSON --outfile sbom.json
      - name: Upload SBOM
        if: always()
        uses: actions/upload-artifact@65462800fd760344b1a7b4382951275a0abb4808
        with:
          name: pulsenet-sbom
          path: sbom.json

  typecheck:
    name: Type Check
    runs-on: ubuntu-latest
    needs: lint
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
      - name: Set up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pyright
      - name: Run Pyright
        run: PYTHONPATH=src pyright src/

  docker:
    name: Docker Build
    runs-on: ubuntu-latest
    needs: lint
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
      - name: Build Docker Image
        run: docker build -t pulsenet:ci --no-cache .
